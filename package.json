{
  "name": "stellar-template",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c cyan,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend run build && cargo test --manifest-path contracts/scholarpass/Cargo.toml && ./test-system.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
